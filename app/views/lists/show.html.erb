<div class="container w-50 mt-5">
  <div class="d-flex justify-content-center mb-3"><h1><%= @list.name %></h1></div>
  <p><%= @list.task_names %></p>

  <ul>
    <% @list.tasks.each do |task| %>
      <li><%= task.name %></li>
    <% end %>
  </ul>

</div>
